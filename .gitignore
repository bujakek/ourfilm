# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# Database dumps taken before a `pnpm reset:data` — guest names, host emails
# and the Early Couple intake all sit in there in plain text.
backup*.sql
*.dump.sql

# TypeScript build artifacts
*.tsbuildinfo
next-env.d.ts
.vercel
.env*

# Vendored Claude Code skill content (~1.4MB), plus the symlinks pointing into
# it. Both are restored by the skill installer from skills-lock.json, which is
# committed — tracking a symlink to an ignored directory just dangles on clone.
.agents/
.claude/skills/
