import { NotFoundScreen } from '@/components/site/not-found-screen'

/** A mistyped or expired event link is the common case here. */
export default function NotFound() {
  return <NotFoundScreen />
}
