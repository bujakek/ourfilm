.next/
node_modules/
pnpm-lock.yaml
public/

# Generated by `pnpm types:gen` — formatting it makes types:check diff
# against the generator output and fail on quote style alone.
lib/supabase/database.types.ts

# Email HTML. Prettier reflows the VML inside the Outlook conditional comment
# and reformats table markup whose whitespace is load-bearing in some clients;
# these are hand-authored and tested as written.
supabase/templates/*.html
